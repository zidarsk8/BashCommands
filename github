SETUP:
1. Generate public key: http://help.github.com/linux-key-setup/
x. If you don't want to type in your password everytime:
$ ssh-add ~/.ssh/id_rsa
2. Setup user name, email:
$ git config --global user.name "Smotko"
$ git config --global user.email "ensmotko@gmail.com"
3. Token:
Account Admin/API Token
$ git config --global github.user Smotko
$ git config --global github.token c98e6262ab13d58da96389ab41632f7e

NEW REPO:

$ mkdir new-project
$ cd new-project
$ git init
$ touch README
$ git add README
$ git commit
$ git remote add origin git@github.com:Smotko/new-project.git
$ git push origin master

OLD REPO:
$ mkdir old-project
$ cd old-project
$ git remote add origin git@github.com:Smotko/old-project.git
$ git pull origin master

IGNORE FILES:
git config --global core.excludesfile ~/dev/.gitignore 
